package com.serwylo.trivia

/**
 * Capable of producing a series of demo TriviaNight's that we can offload on people to
 * show what the site can do.
 */
class DemoService {

	/**
	 * Constructs a bunch of demo trivia nights, and only does so once. This allows us to quickly
	 * pull them up each time somebody asks to view a demo, rather than generating one on the fly.
	 * Additionally, it gives us control to make sure that the demo trivia nights are very nice.
	 */
	def init() {



	}
}
